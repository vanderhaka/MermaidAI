# QA Review: MermaidAI MVP

Generated by `/tdd-deep` pipeline. Each issue appends a QA card below.

---

## Issue 3: Project type defines user-owned container

| Field           | Value                     |
| --------------- | ------------------------- |
| **Commit**      | `d73ad22`                 |
| **Test file**   | `src/types/graph.test.ts` |
| **Source file** | `src/types/graph.ts`      |
| **Tests**       | 3 passed, 0 failed        |
| **tsc**         | 0 errors in graph files   |
| **Status**      | PASS                      |

**What was tested:**

- `Project` type has all 6 required fields (`id`, `user_id`, `name`, `description`, `created_at`, `updated_at`)
- `CreateProjectInput` omits server-generated fields (`id`, `user_id`, `created_at`, `updated_at`)
- `description` is nullable (`string | null`)

**RED phase evidence:** `tsc --noEmit` reported 6 type errors against the incomplete stub (missing `user_id`, `description`, `created_at`, `updated_at`).

---

## Issue 1: Env config validates required env vars

**Commit**: `f49c2f7` | **Type**: feature | **Status**: Implemented

### Summary

Added Zod v4-based environment variable validation that fails fast with clear errors if required vars are missing or empty. Exports `createConfig()` for testing and `getConfig()` as a lazy singleton for runtime use.

### Steps to test

1. This is internal infrastructure — no user-facing UI
2. A developer can verify by removing a required env var from .env.local and starting the app
3. The app should fail to start with a clear error naming the missing variable

### Expected result

App crashes on startup with a ZodError that names the missing variable

### Edge cases

- Empty string values are rejected, not just undefined
- Whitespace is trimmed before validation (handles `vercel env pull` trailing newlines)
- All 4 required vars must be present simultaneously

### Test results

- 8 tests passed, 0 failed
- Test file: `src/lib/config.test.ts`
- Source file: `src/lib/config.ts`
- tsc: 0 errors in config files

### Preserved behaviors

- None (new feature)

---

## Issue 2: Database types placeholder exports typed table definitions

| Field           | Value                        |
| --------------- | ---------------------------- |
| **Commit**      | `567349d`                    |
| **Test file**   | `src/types/database.test.ts` |
| **Source file** | `src/types/database.ts`      |
| **Tests**       | 8 passed, 0 failed           |
| **tsc**         | 0 errors in database files   |
| **Status**      | PASS                         |

**What was tested:**

- `Database['public']['Tables']` contains all 7 required tables: `projects`, `modules`, `flow_nodes`, `flow_edges`, `module_connections`, `chat_messages`, `profiles`
- Each table has `Row`, `Insert`, `Update` subtypes with expected column keys
- Type-level assertions via `keyof` — invalid column names cause `tsc` compilation errors

**RED phase evidence:** `tsc --noEmit` reported 32 type errors against the stub (only `projects` with `id` field). Missing tables and columns were caught at the type level.

**How to verify:**

1. Run `npx tsc --noEmit` — zero errors in `src/types/database.ts` and `src/types/database.test.ts`
2. Run `npx vitest run src/types/database.test.ts` — all 8 tests pass
3. Import `Database` type in any file and verify autocomplete provides all 7 tables with their subtypes
4. File header comment confirms it will be replaced by `supabase gen types typescript`

---

## Issue 18: Database migrations — 7 core tables with FKs, RLS, triggers

| Field           | Value                                                       |
| --------------- | ----------------------------------------------------------- |
| **Commit**      | `b2c2098`                                                   |
| **Test file**   | `src/__tests__/migrations.test.ts`                          |
| **Source file** | `supabase/migrations/20260406000000_create_core_tables.sql` |
| **Tests**       | 30 passed, 0 failed                                         |
| **Status**      | PASS                                                        |

**What was tested:**

- All 7 CREATE TABLE statements present (profiles, projects, modules, flow_nodes, flow_edges, module_connections, chat_messages)
- Foreign keys: profiles->auth.users (CASCADE), projects->auth.users, modules->projects (CASCADE), flow_nodes->modules (CASCADE), flow_edges->modules/flow_nodes (CASCADE), module_connections->projects (CASCADE) + modules, chat_messages->projects (CASCADE)
- RLS enabled on all 7 tables with at least one policy each
- `handle_new_user` trigger function + auth.users INSERT trigger for profile auto-creation
- `set_updated_at` trigger function + triggers on profiles, projects, modules, flow_nodes

**Steps to QA:**

1. Run `supabase db reset` locally
2. Open Supabase Studio and verify all 7 tables appear under `public` schema
3. Check each table has the expected columns and types
4. Verify RLS is enabled on each table (padlock icon in Studio)
5. Check triggers exist under each table's trigger tab

**RED phase evidence:** Stub SQL with only profiles + projects tables caused 23 of 30 tests to fail.

---

## Issue 9: Zod schema validates CreateProjectInput

| Field           | Value                             |
| --------------- | --------------------------------- |
| **Commit**      | `b839021`                         |
| **Test file**   | `src/lib/schemas/project.test.ts` |
| **Source file** | `src/lib/schemas/project.ts`      |
| **Tests**       | 11 passed, 0 failed               |
| **tsc**         | 0 errors in schema files          |
| **Status**      | PASS                              |

**What was tested:**

- Valid input with name only passes
- Valid input with name + description passes
- Null description accepted (nullable)
- Missing description accepted (optional)
- Empty name rejected with error on `name` path
- Whitespace-only name rejected (trim then min(1))
- Name >100 chars rejected; exactly 100 chars accepted
- Whitespace trimmed from name (`"  Test  "` -> `"Test"`)
- Extra fields stripped from output

**RED phase evidence:** Stub schema with bare `z.string()` and non-nullable `description` caused 6 of 11 tests to fail (empty name passed, no trimming, no max length, null description rejected).

---

## Issue 8: Chat types — messages, operations, and context

| Field           | Value                    |
| --------------- | ------------------------ |
| **Commit**      | `3a4a110`                |
| **Test file**   | `src/types/chat.test.ts` |
| **Source file** | `src/types/chat.ts`      |
| **Tests**       | 21 passed, 0 failed      |
| **tsc**         | 0 errors in chat files   |
| **Status**      | PASS                     |

**What was tested:**

- `ChatMessage` has all 5 required fields (`id`, `role`, `content`, `operations`, `createdAt`)
- `ChatRole` is union of `'user' | 'assistant' | 'system'`
- `operations` accepts `GraphOperation[]` with correct discriminated payloads
- `GraphOperationType` is a 10-member union covering all operation types
- All 10 `GraphOperation` variants tested: `create_module`, `update_module`, `delete_module`, `create_node`, `update_node`, `delete_node`, `create_edge`, `update_edge`, `delete_edge`, `connect_modules`
- Each variant has correct `type` discriminant and `payload` shape
- `ChatContext` has `projectId`, `projectName`, `activeModuleId` (nullable), `mode`, `modules`
- `ChatMode` is union of `'discovery' | 'module_map' | 'module_detail'`
- `modules` is `ModuleSummary[]` with `id` and `name`
- `CreateChatMessageInput` requires `project_id`, `role`, `content` — no `id` or `createdAt`

**RED phase evidence:** `tsc --noEmit` reported 28 type errors against the incomplete stub — 9 missing operation exports, missing `createdAt` on `ChatMessage`, `'system'` not in `ChatRole`, missing `activeModuleId`/`modules` on `ChatContext`, `'module_detail'` not in `ChatMode`, missing `project_id` on `CreateChatMessageInput`.

---

## Issue 4: Module type with entry/exit points and position

| Field           | Value                     |
| --------------- | ------------------------- |
| **Commit**      | `b139cf3`                 |
| **Test file**   | `src/types/graph.test.ts` |
| **Source file** | `src/types/graph.ts`      |
| **Tests**       | 11 passed, 0 failed       |
| **tsc**         | 0 errors in graph files   |
| **Status**      | PASS                      |

**What was tested:**

- `Position` type has `x` and `y` number coordinates, including zero and negative values
- `Module` type has all 10 required fields (`id`, `project_id`, `name`, `description`, `position`, `color`, `entry_points`, `exit_points`, `created_at`, `updated_at`)
- `Module.description` is nullable (`string | null`)
- `Module.position` uses the reusable `Position` type (`{ x: number, y: number }`)
- `Module.entry_points` and `exit_points` are `string[]`, including empty arrays
- `CreateModuleInput` omits server-generated fields (`id`, `created_at`, `updated_at`)

**RED phase evidence:** `tsc --noEmit` reported 3 type errors — `Position`, `Module`, and `CreateModuleInput` not exported from `@/types/graph`.
