# Agent Run: M7 main 2026-06-08

Status: DONE
Worker: main orchestrator
Started: 2026-06-08
Completed: 2026-06-08

## Scope

- Node: M7 - Fallow calibration, focused proof, and browser smoke.
- Allowed write scope: progress/evidence artifacts only.
- Files changed:
  - this evidence file
  - `plans/maintainability-cleanup/progress.md`

## Gates

- Focused tests:
  - `npm run test -- src/lib/services/prd-renderers.test.ts src/lib/canvas/edge-routing.test.ts src/lib/services/selected-open-question.test.ts src/app/api/chat/route.test.ts src/lib/services/llm-tools-scoping.test.ts src/components/dashboard/tool-event-applier.test.ts src/components/dashboard/scope-workspace.test.tsx src/lib/canvas/flow-edge-style.test.ts` passed: 8 files, 82 tests.
- Typecheck:
  - `npm run type-check` passed.
- Lint:
  - `npm run lint` passed.
- Fallow:
  - `git diff -- <cleanup files> | fallow audit --diff-stdin --format compact` passed.
  - Audit reported no introduced issues; inherited findings remain for `renderFlowSection`, `orthogonalizePoints`, and `collapseLinearPoints`.
- Browser Gate:
  - Started `npm run dev`; Next.js served `http://localhost:3000`.
  - In-app browser loaded the public page, dashboard, and existing scope workspace route.
  - Workspace rendered `Scope`, the flow canvas, 3 open questions, `Requirements`, `Switch to Full Design`, and the assistant shell.
  - Opened assistant shell without submitting text; open question count remained 3.
  - Browser console errors: none.
- Boundary/Migration Gate:
  - No package lock, database, migration, provider, or live-data changes.
  - Removed accidental untracked `pnpm-lock.yaml` and `pnpm-workspace.yaml` produced by the worker's failed pnpm attempt.

## Behavior Preservation

- Previous intended behaviors preserved: PRD markdown content, public export consumers, edge routing public helpers, selected-question truth, LLM scoping guard, workspace open-question rendering, assistant shell, type safety, lint, and Fallow audit gate.
- Intentional behavior changes:
  - Private helper exports removed from PRD and edge-routing modules.
  - Route/context, selected-question prompt detection, and workspace tool-event switch logic extracted into smaller helpers.
- Confidence: 94%.

## Residual Risk

- M2 was completed locally after Planck timed out and the close request was interrupted.
- Browser smoke avoided submitting a chat/tool request to prevent mutating the local project; unit tests cover those behavior-preserving paths.

## Recommendation

DONE
