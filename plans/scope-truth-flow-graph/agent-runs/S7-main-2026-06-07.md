# S7 Main Run: Recommendation And Accept-Action Contract

## Scope

Ensure the chat's recommended-answer accept path keeps the selected open-question identity, so accepting or overriding resolves the intended question instead of relying on text guessing.

## Files Changed

- `src/components/dashboard/scope-workspace.tsx`
- `src/components/dashboard/scope-workspace.test.tsx`

## Implementation Notes

- Added short-lived `activeResolutionQuestion` state after selecting an open question from the drawer.
- The initial click-only request keeps the selected question active.
- The next accepted suggestion or typed override automatically sends `context.resolvingOpenQuestion` with the original question ID.
- The active selected question clears after a real answer send or after a matching `resolve_open_question` tool event.

## Verification

- GREEN: `npm run test -- src/components/dashboard/scope-workspace.test.tsx src/components/chat/ChatMessageList.test.tsx`
- Focused regression: `npm run test -- src/components/canvas/OpenQuestionsPanel.test.tsx src/components/dashboard/scope-workspace.test.tsx src/lib/canvas/graph-invariants.test.ts src/components/canvas/views/ModuleDetailView.test.tsx src/lib/canvas/flow-edge-style.test.ts src/app/api/chat/route.test.ts src/lib/services/prompt-builder.test.ts src/lib/services/llm-tools-scoping.test.ts src/components/chat/ChatMessageList.test.tsx`
- Repo gates: `npm run type-check`; `npm run lint`

## Browser Gate

Partial. The in-app browser already verified the selected-question response renders a visible recommended answer and `Accept suggestion` button. I did not click the live `Accept suggestion` button because that would intentionally resolve another persisted question in the current project.

## Confidence

Behavior preservation confidence: 91%.
