# S5 Main Run: Question-Node Rendering And Layout Isolation

## Scope

Make open-question nodes readable on the flow board and stop them from distorting process-flow fit/zoom.

## Files Changed

- `src/components/canvas/views/ModuleDetailView.tsx`
- `src/components/canvas/views/ModuleDetailView.test.tsx`

## Implementation Notes

- Passed `data.question` into React Flow question nodes, instead of only `label` and `pseudocode`.
- Added fit-view options that exclude `question` nodes when process nodes exist, so annotation/question cards do not drive the primary viewport framing.

## Verification

- RED: targeted test failed on missing `data.question` and missing fit-target node filtering.
- GREEN: `npm run test -- src/components/canvas/views/ModuleDetailView.test.tsx`
- Focused regression: included in the combined 9-file Vitest run.
- Repo gates: `npm run type-check`; `npm run lint`
- Browser gate: in-app browser showed three amber question nodes with visible question snippets rather than bare `?` markers. Console warning/error logs were empty.

## Confidence

Behavior preservation confidence: 92%.
