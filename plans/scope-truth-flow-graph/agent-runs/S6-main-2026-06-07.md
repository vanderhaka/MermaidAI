# S6 Main Run: Decision Branch Routing And Labels

## Scope

Make decision edges infer the right source handles for business labels beyond literal yes/no.

## Files Changed

- `src/lib/canvas/flow-edge-style.ts`
- `src/lib/canvas/flow-edge-style.test.ts`

## Implementation Notes

- Extended decision-source handle inference for labels such as `Valid`, `Invalid`, `Payment successful`, `Payment failed`, `fail`, `error`, and similar success/failure terms.
- Kept recovery actions such as `Retry` and `Skip` from being forced into yes/no handles.

## Verification

- RED: targeted test failed because `Valid` and similar labels returned no decision handle.
- GREEN: `npm run test -- src/lib/canvas/flow-edge-style.test.ts`
- Focused regression: included in the combined 9-file Vitest run.
- Repo gates: `npm run type-check`; `npm run lint`
- Browser gate: in-app browser showed `Condition: Valid` on the positive branch and `Condition: Invalid` / `Condition: fail` on the negative branches with the expected styling. Console warning/error logs were empty.

## Confidence

Behavior preservation confidence: 90%.
