# S10 Main Run: Full Proof And Behavior-Preservation Report

## Completed Slices

- S1: ID-bound open-question resolution trigger.
- S2: Graph invariant detector and regression fixture.
- S3: Tool-backed resolved-state truth contract.
- S4: Flow edit repair and graph-connectivity guards.
- S5: Question-node rendering and layout isolation.
- S6: Decision branch routing and labels.
- S7: Recommendation and accept-action contract.
- S8: Chat docking and resize polish.

## Final Verification

- `npm run test -- src/components/canvas/OpenQuestionsPanel.test.tsx src/components/dashboard/scope-workspace.test.tsx src/lib/canvas/graph-invariants.test.ts src/components/canvas/views/ModuleDetailView.test.tsx src/lib/canvas/flow-edge-style.test.ts src/app/api/chat/route.test.ts src/lib/services/prompt-builder.test.ts src/lib/services/llm-tools-scoping.test.ts src/components/chat/ChatMessageList.test.tsx src/components/chat/FloatingChat.test.tsx`
  - Result: 10 files passed, 174 tests passed.
- `npm run type-check`
  - Result: passed.
- `npm run lint`
  - Result: passed.
- In-app browser checks on `/dashboard/9375bc1a-a0ba-4719-a18d-70609ce147d0`
  - Selected-question click-only resolve now keeps open-question count at `3`, leaves the selected question listed, renders a question and recommended answer, and shows `Accept suggestion`.
  - Question nodes render readable question text instead of bare `?`.
  - Decision labels render valid/invalid/failure branches with the expected styling.
  - Assistant chat opens at the wider size and exposes resize controls.
  - Browser console warning/error logs were empty during final checks.

## Intentional Behavior Changes

- Selecting an open question no longer lets the model resolve it from stale history or click-only wording.
- Click-only selected-question requests short-circuit to a deterministic question plus recommended answer when the DB still says the question is open.
- Accepting or overriding a selected-question recommendation carries the selected question ID through the next chat request.
- Flow edit tools now surface graph invariant issues back to the model so it can repair disconnected islands and contradictory paths.
- Chat default size is wider/taller and remembers user resizing.

## Preserved Behaviors

- Existing chat streaming remains intact.
- Tool events still update nodes, edges, modules, PRD, and open questions in the graph store.
- Grouped open-question drawer remains the entry point for resolving questions.
- Recommendation accept buttons still send `Accept suggestion: ...` as normal user input.
- Canvas pan/zoom and process-node rendering remain unchanged except for question-node text and fit isolation.

## Known Blocked Item

- S9 current-project data repair remains blocked. The current project has known test-data drift, including the cart-editing question removed by the earlier pre-guard smoke test and graph edges that still need persisted repair. I did not mutate that project data without explicit approval.

## Confidence

Behavior preservation confidence: 91%.
