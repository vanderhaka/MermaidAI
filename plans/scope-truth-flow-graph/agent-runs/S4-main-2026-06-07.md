# S4 Main Run: Flow Edit Repair And Graph-Connectivity Guards

## Scope

Give the assistant actionable graph-health feedback after flow edits so it repairs disconnected islands, missing decision branches, and contradictory failure paths before replying.

## Files Changed

- `src/lib/services/llm-tools.ts`
- `src/lib/services/llm-tools-scoping.test.ts`
- `src/lib/services/prompt-builder.ts`
- `src/lib/services/prompt-builder.test.ts`

## Implementation Notes

- Added post-edit graph checks to `create_node`, `update_node`, and `create_edge` tool results.
- Reused `validateFlowGraph` so graph feedback is based on the same invariant fixture as the review finding.
- Tool results now include `Graph check: ... Repair before replying: ...` when issues remain, plus structured `graphIssues` data.
- Added prompt rules for rewiring inserted steps, avoiding disconnected islands, and avoiding terminal-plus-retry negative branches.

## Verification

- RED/GREEN: added executor test proving broken graph state returns `Graph check:` feedback with an `unreachable_node` issue.
- GREEN: `npm run test -- src/lib/services/llm-tools-scoping.test.ts src/lib/services/prompt-builder.test.ts src/lib/canvas/graph-invariants.test.ts`
- Focused regression: `npm run test -- src/components/canvas/OpenQuestionsPanel.test.tsx src/components/dashboard/scope-workspace.test.tsx src/lib/canvas/graph-invariants.test.ts src/components/canvas/views/ModuleDetailView.test.tsx src/lib/canvas/flow-edge-style.test.ts src/app/api/chat/route.test.ts src/lib/services/prompt-builder.test.ts src/lib/services/llm-tools-scoping.test.ts src/components/chat/ChatMessageList.test.tsx`
- Repo gates: `npm run type-check`; `npm run lint`

## Browser Gate

Skipped for this slice: S4 changes server-side tool feedback and exercising them in the visible app would require additional live graph mutations. S3/S5/S6 browser checks already verified the current visible board, drawer, and selected-question flow.

## Confidence

Behavior preservation confidence: 88%.
